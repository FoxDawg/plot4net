<Window
    x:Class="TestApplication.Core.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:core="clr-namespace:TestApplication.Core"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MainWindow"
    Width="500"
    Height="500"
    d:DataContext="{d:DesignInstance core:MainWindowViewModel}"
    mc:Ignorable="d">
    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <Button
                Width="100"
                Height="50"
                Margin="2"
                Command="{Binding PlotCommand}">
                Draw
            </Button>
            <Button
                Width="100"
                Height="50"
                Margin="2"
                Command="{Binding ExportCommand}">
                Export
            </Button>
        </StackPanel>


        <core:FigureControl
            Width="400"
            Height="400"
            Figure="{Binding Figure}" />
    </StackPanel>
</Window>